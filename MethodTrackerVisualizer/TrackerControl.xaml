<UserControl x:Class="MethodTrackerVisualizer.TrackerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:stepByStepLogger="clr-namespace:StepByStepLogger"
             Foreground="White"
                        mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400"
             Background="#FF1E1E1E">
    <UserControl.Resources>
        <!-- Converter for list to string -->
        <stepByStepLogger:ListToStringConverter x:Key="ListToStringConverter"/>

        <!-- Base style for all text -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <!-- Style for Buttons -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderBrush" Value="#FF3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3E3E42" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF515154"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderBrush" Value="#FF3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
        </Style>

        <!-- Style for TreeView -->
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Style for TreeViewItem -->
        <Style TargetType="TreeViewItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="Expander"
                            Style="{StaticResource {x:Static ToolBar.ToggleButtonStyleKey}}"
                            IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                            Visibility="{Binding Path=HasItems, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Width="16" Height="16"
                            Margin="2,0,2,0"/>
                            <Border x:Name="Bd"
                      Grid.Column="1"
                      Background="Transparent"
                      Padding="2">
                                <ContentPresenter x:Name="PART_Header"
                                  ContentSource="Header"/>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost"
                              Grid.Column="1" Grid.Row="1"
                              Margin="19,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF3E3E42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- HierarchicalDataTemplate for LogEntry -->
        <HierarchicalDataTemplate DataType="{x:Type stepByStepLogger:LogEntry}" ItemsSource="{Binding Children}">
            <Border BorderThickness="1" BorderBrush="#FF3E3E42" Padding="5" Margin="2" Background="Transparent" CornerRadius="3">
                <StackPanel>
                    <TextBlock Text="{Binding MethodName}" FontWeight="Bold" FontSize="14"/>
                    <TextBlock>
            <Run Text="Parameters: " FontWeight="Bold"/>
            <Run Text="{Binding Parameters, Converter={StaticResource ListToStringConverter}}" />
                    </TextBlock>
                    <TextBlock>
            <Run Text="Return Type: " FontWeight="Bold"/>
            <Run Text="{Binding ReturnType}" />
                    </TextBlock>
                    <TextBlock>
            <Run Text="Return Value: " FontWeight="Bold"/>
            <Run Text="{Binding ReturnValue}" />
                    </TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Start: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding StartTime}" Margin="2,0,5,0"/>
                        <TextBlock Text="End: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding EndTime}" Margin="2,0,5,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Elapsed: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding ElapsedTime}" Margin="2,0,5,0"/>
                        <TextBlock Text="Exclusive: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding ExclusiveElapsedTime}" Margin="2,0,5,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </HierarchicalDataTemplate>

        <!-- BooleanToVisibilityConverter, can also be defined in code -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Search bar row -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
            <TextBox x:Name="SearchTextBox" 
               Width="250" 
               Margin="0,0,10,0" 
               VerticalAlignment="Center" 
               TextChanged="SearchTextBox_TextChanged"/>
            <Button Content="Previous" Click="PreviousButton_Click" VerticalAlignment="Center"/>
            <Button Content="Next" Click="NextButton_Click" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <Button Content="Clear" Click="ClearButton_Click" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
        <!-- Log tree view row -->
        <TreeView x:Name="LogTreeView" Grid.Row="1" Margin="10" Background="Transparent"/>
    </Grid>
</UserControl>