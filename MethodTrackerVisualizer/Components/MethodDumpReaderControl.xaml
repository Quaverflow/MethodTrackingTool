<UserControl x:Class="MethodTrackerVisualizer.Components.MethodDumpReader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:methodTrackerVisualizer="clr-namespace:MethodTrackerVisualizer"
             xmlns:components="clr-namespace:MethodTrackerVisualizer.Components"
             xmlns:views="clr-namespace:MethodTrackerVisualizer.Views"
             Background="Black">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <LinearGradientBrush x:Key="HeaderGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF800080" Offset="0"/>
            <GradientStop Color="#FF000000" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>
    <Grid Background="Black">
        <TabControl>
            <TabItem Header="Chronological">
                <views:ChronoTree x:Name="ChronoTreeControl" DockPanel.Dock="Bottom"/>
            </TabItem>
            <TabItem Header="Exclusive">
                <views:ExclusiveList x:Name="ExclusiveListControl"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>