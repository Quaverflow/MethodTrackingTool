<UserControl x:Class="MethodTrackerVisualizer.Components.MethodDumpReader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:MethodTrackerVisualizer.Views"
             Background="Black">
    <UserControl.Resources>
        <Style TargetType="TreeViewItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

    </UserControl.Resources>
    <Grid Background="Black">
        <TabControl>
            <TabItem Header="Chronological">
                <views:ChronoTree x:Name="ChronoTreeControl" DockPanel.Dock="Bottom"/>
            </TabItem>
            <TabItem Header="Exclusive">
                <views:ExclusiveList x:Name="ExclusiveListControl"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>